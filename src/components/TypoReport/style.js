// Styles migrated to sx prop - this file kept for backward compatibility
const useStyles = () => ({
  backdrop: {
    zIndex: 'drawer',
    color: '#fff',
  },
  errorMessage: {
    color: 'red',
  },
  ref: {
    textAlign: 'right',
    fontWeight: 'bold',
  },
  finishDialogContent: {
    textAlign: 'center',
  },
});

export default useStyles;
