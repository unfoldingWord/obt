// Styles migrated to sx prop - this file kept for backward compatibility
const useStyles = () => ({
  linkContainer: {
    cursor: 'pointer',
    padding: '4px 16px',
    display: 'block',
  },
});
export default useStyles;
