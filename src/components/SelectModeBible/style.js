// Styles migrated to sx prop - this file kept for backward compatibility
export const useStyles = () => ({
  formControl: {
    width: '100%',
    marginRight: 2,
  },

  icon: { color: 'inherit' },

  select: {
    fontSize: '0.875rem',
    fontWeight: '500',
  },
});
